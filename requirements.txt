# Dependencias del agente multimodal (documentos académicos).
# Recomendado: Python 3.11 o 3.12 (mejor disponibilidad de "wheels").
#
# El modelo de lenguaje NO es una dependencia pip: se ejecuta en local con
# Ollama (ver README). Aquí solo van las librerías de Python.

# --- Orquestación LLM (LangChain + Ollama) ---
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-ollama>=0.2,<0.4

# --- Validación / salida estructurada ---
pydantic>=2.7

# --- Ingesta de documentos ---
pymupdf>=1.24        # PDF (import fitz)
pandas>=2.2          # CSV
tabulate>=0.9        # para df.to_markdown()
pillow>=10.3         # imágenes (ruta multimodal opcional)

# --- Interfaz ---
streamlit>=1.36      # demo en vivo
